import 'package:flutter/material.dart';

class QuestPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: const Text("Quests"),
    ),
    body: const Center(child: Text("Quest Page")),
  );
}